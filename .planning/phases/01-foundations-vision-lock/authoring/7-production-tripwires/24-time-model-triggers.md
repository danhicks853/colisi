---
author: Daniel
end_product: Production Tripwires — the canonical list of player actions that advance time. Locks D-TIME-05 trigger list; feeds Phase 4 implementation.
canonical_path: .planning/phases/01-foundations-vision-lock/deliverables/decision-lock-pack/24-time-model-lock.md (Claude populated D-TIME-01..04 contract sections; this doc fills D-TIME-05 trigger list)
source_decisions: D-TIME-01..05
input_dependencies:
  - 05-core-loops.md (trigger list section in core loops feeds this doc — derive from your loops work)
length_target: ~500–1000 words; complete trigger list with classification
required_per_trigger:
  - Action name (player verb — your invention)
  - Trigger category (movement / interaction / dialogue / observation / "passive nothing")
  - Time advance amount (minutes / hours / "fade to next" segment)
  - Justification (why does this action advance time? what makes it diegetically time-passing?)
  - Edge cases (what about cancellation mid-action? what about queuing?)
hard_guardrails:
  - Pure action-driven contract per D-TIME-01 — NO wall-clock advancement; only player actions advance time
  - Player-controlled day/night per D-TIME-02 — explicit "rest until dawn" / "rest until dusk" actions exist; passing time without player consent is forbidden
  - "Passive nothing" category is interesting — does idle observation advance time? if yes, what's the trigger? (player-initiated meditation? sitting?)
  - Triggers must be derivable from your core loops in 05-core-loops.md — if a trigger here isn't in core loops, that's a sign the loops doc is incomplete
  - Defer-to-Phase-4 list: anything you're unsure about, list under "Open trigger questions" rather than guess
order: write after 05-core-loops.md lands; can run parallel with 21-year3wall-thresholds and 23-why-play-hour-200
---

# Time-Model Trigger List (D-TIME-05)

## Trigger taxonomy

## Trigger list — canonical actions that advance time

### Movement triggers

### Interaction triggers

### Dialogue triggers

### Observation triggers

### Rest / day-advance triggers

## Edge-case handling

## Open trigger questions (deferred to Phase 4)

## Cross-references

*(Claude populates once draft lands.)*
