# Art Skill-Acquisition Learning Log

> Session-by-session progress record. Each entry: date, syllabus module, what was built/learned, style-direction checks (pass/fail), next session's target. Append-only.

---

## 2026-05-11 — Session 01: Syllabus creation

- **Syllabus phase / module:** Pre-Phase 1 (planning)
- **Goal:** Build a focused skill-acquisition syllabus targeting Colisi's specific asset deliverables.
- **What was built:** [`syllabus.md`](syllabus.md) v0.2 — 9 phases, each module tracing to one of 10 named deliverables (A-J). v0.1 was broader (general-art-skill curriculum); Daniel directed "bass not all fish" focus; v0.2 cut everything not directly enabling a Colisi asset.
- **Technique learned:** N/A — planning session, not technique.
- **Style-direction checks:** N/A.
- **AI policy notes:** Confirmed Claude's role across the syllabus is teach/critique/locate-resources only. All creative content (designs, palettes, names, references) is Daniel-authored.
- **Next session target:** Phase 1.1 — Reference workflow (PureRef setup). First real tutorial module.

---

## 2026-05-12 — Session 02: Phase 1.1 — Reference workflow (PureRef anchor board)

- **Syllabus phase / module:** Phase 1.1 — Reference workflow (PureRef setup).
- **Goal:** Build PureRef anchor reference board covering all four locked-style anchors with ≥10 references + articulated observations.
- **What was built:** PureRef anchor board (`D:\Projects\art-scratch\reference\anchors.pur`) — ~12-13 references spanning Sandara Tang, Hades/Jen Zee, Studio Ghibli, Okami, plus parallel-anchor Ni no Kuni additions. Articulations captured in [`technique-notes.md`](technique-notes.md) §Phase 1.1.
- **Tooling confirmed:** Apple Pencil Gen 2 + iPad available with native pressure sensitivity → unblocks painted-texture workflow at $0-13 cost (Procreate native). Streaming-to-PC via Weylus available as supplementary tool if needed later. Pressure-input ceiling for the locked style is no longer a concern.
- **Technique learned:**
  - PureRef interface (drag-drop, pan, zoom, save).
  - Eye-training principle: identifying *patterns* across an anchor (e.g., Ghibli's "less detail = more emotion") matters more than per-image inventory.
  - Tang's tiger-and-cub piece is a fair direction-anchor for Zhan (leonid → same big-cat family) but lion-specific reference work happens at Phase 9.
- **Style-direction checks:**
  - ✅ All four named anchors represented on the board.
  - ✅ ≥10 references.
  - ✅ Articulation captured for each anchor (Tang, Hades, Ni no Kuni, Okami, Ghibli).
  - ⚠️ Board is character-heavy; environment-focused references thin. Daniel added more environment imagery in the second pass; further environment study refs to accumulate as Phase 6 approaches.
  - ⚠️ Ni no Kuni added as parallel anchor; not in canonical mood deck. Personal study board use is fine; canonicalizing would require SCOPE.md mood-deck amendment.
- **AI policy notes:** All references curated by Daniel. Claude critiqued completeness against locked anchors (mood deck §) and articulation depth; Claude did NOT propose specific reference images.
- **Phase 1.1 status:** ✅ Complete.
- **Next session target:** Phase 1.2 — Light + value for cel-shading (5 sphere studies, pencil + paper).

---

## 2026-05-12 — Session 03: Phase 1.2 — Light + value (sphere studies)

- **Syllabus phase / module:** Phase 1.2 — Light + value for cel-shading.
- **Goal:** Build the eye for the six light regions on a sphere across 5 lighting scenarios; articulate where cel-shading bands would fall.
- **Tooling decision:** Daniel chose iPad + Procreate (graphite-style brushes, grayscale) over pencil + paper. Syllabus authorized this path. Apple Pencil Gen 2 with pressure confirmed working.
- **What was built:** 6 sphere studies (5 scenarios; overhead scenario was iterated twice as the "no outline" lesson landed). Saved in `art-scratch/01-foundations/spheres/`. Studies progressed:
  - **Study 1 (overhead):** outlined circle; no cast shadow; no highlight. Foundational issue identified — drawing the *symbol of a sphere* (2D circle) instead of the *3D form*.
  - **Study 2 (overhead, redo):** outline gone; cast shadow added; broader value range. Core "see form not outline" lesson landed in one iteration.
  - **Study 3 (cool side rim, left):** highlight added (but too large); cast shadow direction correct; value range strong.
  - **Study 4 (warm-cool split, L+R):** highlight compressed to proper size (small specular peak); terminator captured (drawn slightly too crisply — terminators are *bands* not *lines*); two-light interaction visible with one dominant + one fill.
  - **Study 5 (overcast diffuse):** value range correctly compressed; ambient occlusion at top/bottom correctly subtle; NO fake contrast added. Lesson: trust observation, don't fabricate cues that aren't there.
  - **Study 6 (golden-hour rake, light below):** long dramatic cast shadow; all six regions labeled; band-placement articulated for 2-band and 3-band cel-shading.
- **Technique learned:**
  - Edges of forms are **value transitions, not lines.** Outlines flatten the form.
  - Six light regions: cast shadow, form shadow, terminator, half-tone, light, highlight.
  - Highlight is a *peak* (small specular point), not a *region*.
  - Terminator is a *band* (narrow densest region), not a *line*.
  - Contact shadow (sphere meets surface) is typically the darkest spot in any scene.
  - Diffuse light = compressed value range; resist fabricating contrast.
  - **2-band cel:** boundary at terminator (lit = highlight+light+half-tone; shadow = terminator+form-shadow+cast-shadow).
  - **3-band cel:** highlight+light → bright; half-tone → mid; terminator+form-shadow+cast-shadow → shadow.
  - **Colisi style note:** hard band edges (not soft gradients between bands) + painterly accent overlay layer = the synthesis. Half-tone band width is per-asset (narrow = graphic/Hades; wide = dimensional/Ghibli).
- **Style-direction checks:** Band-placement choices map cleanly to cel-shading pipeline in Phase 2.2/2.3. Foundation for every shaded surface in the project established.
- **AI policy notes:** All studies by Daniel. Claude critiqued per study; identified observation patterns + recurring issues (outline reflex, highlight size, terminator-as-line); never proposed specific value placements.
- **Phase 1.2 status:** ✅ Complete.
- **Next session target:** Phase 1.3 — Color theory through the cel-shading lens (anchor analysis: 1 Okami + 1 Sandara Tang + 1 Ghibli still, documented).

---

## 2026-05-12 — Session 04: Phase 1.3 — Color theory anchor analysis

- **Syllabus phase / module:** Phase 1.3 — Color theory through the cel-shading lens.
- **Goal:** Build the color-eye via deep analysis of three anchor references (one each: Okami, Tang, Ghibli) covering value range, color temperature, chroma spike, palette structure.
- **What was built:** Three anchor analyses captured in [`technique-notes.md`](technique-notes.md) §Phase 1.3 (Daniel-authored observations + critique refinements). Three-anchor compositional-strategy framework synthesized (Okami / Tang / Ghibli as three distinct value+chroma+ink-line strategies, all valid for Sterling depending on the scene's emotional register).
- **Technique learned:**
  - **Value range strategy is a choice, not a default.** Full range (Okami/Ghibli) = energetic/dramatic; compressed midtones (Tang) = calm/intimate. Per-scene decision.
  - **Cool-frame / warm-figure** (Tang's signature) is the load-bearing trick for cozy register. Cool surround = recession + safety; warm subject = presence + intimacy.
  - **Atmospheric perspective:** distance desaturates and cools. Used in every painterly anchor.
  - **Chroma spike strategy:** one cranked spike (Okami) vs. one quiet spike via surround-contrast (Tang) vs. multiple rhythmic peaks (Ghibli) — three different focal-attention models, all valid.
  - **Critical Colisi-style validation:** Ghibli's **ink-line on characters only, painterly-no-outline on environments** is the exact technique the locked style commits to. Ghibli is the proof-of-concept anchor for the whole synthesis.
  - **Animation saturation principle:** characters more saturated than backgrounds → figure-ground separation.
- **Style-direction checks:**
  - ✅ The locked-style synthesis (Hades ink-line chars + Okami/Tang painterly envs + cozy/calm register) now has a concrete framework to apply per scene.
  - ✅ Tang's cool-frame technique identified as the load-bearing tool for Sterling's interior cozy scenes.
  - ✅ Okami's high-energy strategy identified as fit for outdoor Sterling (Festival Valley, Town Center daytime).
  - ✅ Ghibli's "lived-in / wandering" focal approach identified as Sterling's overall default register.
- **AI policy notes:** All three references curated by Daniel. All analyses Daniel-authored. Claude refined observations + named techniques (cool-frame, atmospheric perspective, animation saturation principle); Claude did NOT propose specific palette values, named colors, or canonical Colisi references.
- **Phase 1.3 status:** ✅ Complete.
- **Phase 1 (Foundations) status:** ✅ Complete. All three foundations modules (PureRef workflow, sphere studies, color theory) closed.
- **Next session target:** Phase 2.1 — Blender Guru donut tutorial (trial-by-fire of Blender interface). First Blender module; full pipeline tour.

---

## 2026-05-12 — Session 05: Phase 2.1 — Blender Guru donut tutorial

- **Syllabus phase / module:** Phase 2.1 — Blender Guru donut tutorial (full series).
- **Goal:** Trial-by-fire of Blender's whole interface in one project; full pipeline tour from modeling to render.
- **What was built:** Complete donut + icing + sprinkles + plate + scene + Cycles render. Saved at `D:\Projects\art-scratch\02-blender-fundamentals\donut.blend`. Final render shows working material system (translucent icing, matte plate, wood grain), working particle system (sprinkle distribution), grounded lighting/shadow.
- **Technique learned:**
  - **Edit Mode** = vertex/edge/face manipulation (vs. Object Mode = whole-object transforms).
  - **Modifier** = non-destructive effect in a stack; can be edited or removed without altering base mesh. Named instances: subdivision, solidify, shrinkwrap. *(Solidify is the modifier used for ink-outline in Phase 2.2.)*
  - **Shader / material** = recipe controlling surface appearance via parameters (color, roughness, metallic, etc.); built in Shader Editor via node connections.
  - **UV unwrap** = separate process from shader; lays out 3D mesh as flat 2D canvas (like peeling an orange and flattening the peel). Bridges 3D surface to 2D texture work.
  - **Particle system** = scatter/distribute objects across a surface (sprinkles on the icing).
  - **Cycles renderer** = path-traced realistic renderer used for donut. Colisi will switch to **Eevee** (realtime rasterized renderer) from Phase 2.2 onward per STACK.md target (modest-hardware audience). Cel-shading uses Eevee-specific **Shader-to-RGB node**.
- **Style-direction checks:** N/A — donut tutorial is intentionally photoreal-leaning, not stylized. Aesthetic alignment begins at Phase 2.2.
- **AI policy notes:** No creative content authored. Tutorial completion + concept articulation only.
- **Phase 2.1 status:** ✅ Complete.
- **Next session target:** Phase 2.2 — First stylized prop with full cel-shading pipeline (a mushroom). End-to-end: model → topology → UV → hand-paint texture (Procreate) → cel-shade shader → ink outline → painterly accent → Eevee render. Multi-session module.

---

## 2026-05-12 to 2026-05-14 — Sessions 06-N: Phase 2.2 — First stylized prop (mushroom)

- **Syllabus phase / module:** Phase 2.2 — First stylized prop end-to-end pipeline. Multi-session module.
- **Goal:** First Colisi-style-aligned prop, full pipeline: reference → silhouette → block-out → topology → UV → hand-paint texture → cel-shade shader → ink outline → painterly accent → Eevee render.
- **What was built:** A chunky-squat stylized mushroom (pink cap + sage green stem). Saved at `D:\Projects\art-scratch\02-blender-fundamentals\mushroom.blend` (and final render at `renders/mushroom-final.png`). First Colisi-style-aligned asset in the project's history.
- **Pipeline executed end-to-end:**
  - PureRef anchor board (3 refs, all aligned with locked style after initial revision pruned photoreal refs).
  - 3 design silhouette thumbnails in Procreate; picked #2 (chunky-squat with tree-stump base) over #1 (whimsical tall) and #3 (cluster).
  - Block-out: cylinder + UV sphere half. ~800 tris initially.
  - Tri reduction: Decimate modifier to 499 tris (target met). Lesson: Option B (rebuild with fewer initial segments) would have given cleaner topology; Option A (Decimate) is fast-but-pebbly. **Documented as preference for future props.**
  - **Shade Smooth** replaced subsurf as the smoothing pattern — production-ready (no tri inflation, no apply-modifier overhead).
  - UV unwrap: vertical-meridian seams, **deleted invisible top + bottom n-gon caps of stem** (low-poly optimization — saves tris + cleans up UV). Average Islands Scale + Pack with 0.02 margin.
  - Hand-painted texture in Procreate. **Gotcha logged:** hide UV-guide layer before export — first attempt baked UV grid into texture.
  - Cel-shading shader built as **`Cel` Node Group** (Diffuse BSDF → Shader-to-RGB → ColorRamp Constant → Multiply with base color → painterly Noise Texture → Mix → Emission → Output). Saved as Node Group inside the .blend; reusable. **The canonical Colisi material recipe is now a single drag-and-drop.**
  - Solidify modifier for ink outline. **Gotcha logged:** Backface Culling MUST be enabled on the Outline_Black material (in viewport AND render settings) or the shell engulfs the form. Also: **multi-mesh objects produce junction artifacts** — cap+stem joined via Ctrl+J to single mesh resolved it.
  - Painterly accent: Noise Texture + ColorRamp + Multiply inside the Cel group. Subtle 0.85-1.0 range gives hand-touched feel.
  - Eevee render with Area light (1000W, warm, angled) + warm muted background.
- **Pipeline gotchas captured in technique-notes.md:**
  - Mix node must be set to **Multiply blend type at Factor 1.000** (not Mix at 0.5) for canonical cel-shading.
  - **Backface Culling** on outline material — both Viewport AND Render settings.
  - **Single-mesh objects** work cleanly with Solidify; multi-mesh produces junction artifacts. Phase 9 character work needs to plan for this.
  - **Decimate** rescues high-poly into low-poly fast but produces irregular topology; rebuild with low-segment-count primitives is cleaner.
  - **Shade Smooth** > Subdivision Surface for low-poly cel-shaded work (no tri inflation, no apply-modifier debt).
  - **Hide UV-guide layer in Procreate before texture export** (otherwise grid lines bake into texture).
- **Style-direction checks:**
  - ✅ Cel-shaded bands hard-edged (Constant ColorRamp interpolation).
  - ✅ Bold black ink outline visible around silhouette (after multi-mesh→single-mesh fix).
  - ✅ Hand-painted base color preserved through shader (Multiply with cel bands gives darker base-color shadows, not gray-washed).
  - ✅ Painterly accent layer subtle (hand-touched feel, not film grain).
  - ✅ Warm + cool complementary palette (Tang anchor applied).
  - ✅ Reads as cozy-painterly-cel-shaded at viewing distance.
  - ⚠️ Multi-mesh junction artifact discovered & resolved via Ctrl+J (documented).
- **Capability matrix impact (Daniel commits via GSD when ready):**
  - **"Blender 3D modeling"** — significant advance toward Daniel-target. Full prop pipeline executed end-to-end.
  - **"Blender rigging + glTF export"** — N/A this phase (static prop; no rig).
  - New row to propose: **"Cel-shading shader development"** — advanced; canonical Cel Node Group exists as reusable tool.
  - New row to propose: **"Hand-painted texture work (Krita/Procreate)"** — advance; one full texture painted end-to-end.
- **AI policy notes:** All design decisions (silhouette, palette, painterly accent intensity, outline thickness, lighting choice) authored by Daniel. Claude provided pipeline walkthrough + diagnostics + technique vocabulary. Claude did NOT propose specific colors, palette values, mushroom design, or anchor reference images.
- **Phase 2.2 status:** ✅ Complete. **First Colisi-style-aligned asset shipped to art-scratch.** The pipeline is the deliverable; this mushroom is proof.
- **Next session target:** Phase 2.3 — Second stylized prop (wooden mug or stylized lantern) to reinforce the pipeline. Same shader, same workflow, different shape — internalizes the muscle memory.

---

## 2026-05-13 to 2026-05-14 — Sessions 07-N: Phase 2.2 — First stylized prop (mushroom) — closed

- **Syllabus phase / module:** Phase 2.2 — First stylized prop with full cel-shading pipeline.
- **Status:** ✅ Complete.
- **Goal:** First Colisi-style-aligned 3D asset end-to-end (silhouette → model → UV → hand-paint texture → cel-shade shader → ink outline → painterly accent → Eevee render).
- **What was built:** First stylized cel-shaded mushroom (chunky-squat character from Silhouette #2). Saved at `D:\Projects\art-scratch\02-blender-fundamentals\` (.blend + textures + final render).
- **Pipeline established:**
  - Reference workflow (PureRef anchor board → silhouette thumbnails → block-out reference image in Blender viewport).
  - Modeling (low-poly primitives + Edit Mode operations + Decimate for tri reduction + Shade Smooth for normal interpolation).
  - UV unwrap (mark seams on back side → Unwrap Angle Based → Average Islands Scale → Pack Islands → Export UV Layout PNG).
  - Texture painting (Procreate with iPad + Apple Pencil Gen 2; hide UV guide layer before export; PairDrop or PNG-supporting transfer to PC).
  - Material setup (Cel Node Group + Outline_Black material + Solidify modifier).
  - Lighting + render (Area light at 1000W warm + warm muted background + F12 Eevee render).
- **Reusable artifact:** `Cel` Node Group (canonical Colisi-style shader) saved in mushroom.blend; recipe documented in [`technique-notes.md`](technique-notes.md) §Phase 2. Drop on any future asset for the full cel-shading + painterly synthesis.
- **Technique learned (highlights):**
  - **Outline = lines; silhouette = filled mass.** Two different design tools.
  - **Texture provides base color only; shader provides lighting.** Don't paint shadows/highlights into the texture.
  - **Hide UV guide layer in Procreate before export** (otherwise grid lines bake into texture).
  - **JPEG vs PNG**: PNG preferred; JPEG acceptable for learning (use PairDrop to bypass iCloud's PNG restriction).
  - **Decimate tradeoff**: fast tri reduction but ugly topology; Shade Smooth rescues visually without inflating tris.
  - **Solidify outline polarity**: POSITIVE thickness + POSITIVE offset + Flip Normals ON (not negative thickness).
  - **Backface Culling on outline material is mandatory** (in viewport AND render settings).
  - **Viewport-vs-render discrepancy is real**: always verify F12 render output before claiming an asset is done.
  - **Cel-shading band darkness is per-asset taste**: pure black left stop = Hades-leaning dramatic; raised gray = Tang-leaning cozy.
- **Style-direction checks (final render):**
  - ✅ Hard-edged cel bands (locked-style cel-shading direction).
  - ✅ Bold black ink outline (Hades-anchor + Solidify trick).
  - ✅ Warm pink + sage green complementary palette (Tang anchor for cool-frame logic).
  - ✅ Painterly accent visible as subtle hand-touched feel (Okami anchor).
  - ✅ Silhouette readability preserved through whole pipeline.
  - ⚠️ Render leans more Hades-dramatic than cozy-Tang (deep shadows near pure black). Acceptable for learning prop; would tune lighter for slice cast and Sterling interiors per Tang strategy.
- **AI policy notes:** All design + palette + reference decisions Daniel-authored. Claude taught technique, critiqued workflow + style-direction alignment, located human-authored tutorials. Did NOT propose specific palette values, reference images, or design choices.
- **Capability matrix implications:** GDScript syllabus row "Blender 3D modeling" + "Blender rigging + glTF export" + new row "Cel-shading shader development" advancing toward Daniel-target. Draft amendment for `16-capability-gap-matrix.md` pending when Daniel returns to main GSD workflow.
- **Phase 2.2 status:** ✅ Complete. First Colisi-style-aligned asset shipped to art-scratch milestone record.
- **Phase 2 status:** Phase 2.1 (donut) + Phase 2.2 (mushroom) both closed. Remaining: Phase 2.3 — Second stylized prop (mug/lantern) to internalize the pipeline at second pass.
- **Next session target:** Phase 2.3 — Second stylized prop (wooden mug OR stylized lantern). Repeat the full pipeline as confirmation that the workflow is reflexive. Likely 30-50% faster than the mushroom now that the recipe is locked.

---

## 2026-05-15 to 2026-05-16 — Session 08-N: Phase 2.3 — Second stylized prop (sign-lamp)

- **Syllabus phase / module:** Phase 2.3 — Second stylized prop with full cel-shading pipeline + three new technique extensions.
- **Status:** ✅ Complete.
- **Goal:** Internalize the cel-shading prop pipeline at second pass AND extend it with three new techniques never used in mushroom: multi-mesh hierarchy, emissive lighting, authored animation.
- **What was built:** Magic-receding wooden sign-lamp (silhouette #3 variant). First Colisi-style-aligned **animated** prop in the project. Saved at `D:\Projects\art-scratch\02-blender-fundamentals\lamppost\` (.blend + textures + day-lit and night-lit Eevee renders).

- **Design exploration (3 silhouette thumbnails, distinct damage philosophies):**
  - **#1 (rejected):** Cross-arm asymmetric post + broken bracket + upside-down lantern + half-detached sign — "village pragmatism, broken physical infrastructure"
  - **#2 (rejected):** Tree-trunk organic post + broken branch + sign nailed to trunk (salvaged) + rope-improvised lantern mount — "village pragmatism, nature-touched"
  - **#3 (WON, built):** Slim slender wooden post + Archimedean wooden scroll bracket + open-frame wooden-tine lantern with cracked tines + cracked post wood + green grass at base — "broken MAGIC infrastructure, magic receding from the world"
  - Alternates #1 and #2 captured in [`technique-notes.md`](technique-notes.md) and [`handback-to-main-session.md`](handback-to-main-session.md) for future variant work.

- **Design DNA committed to (must survive every progression version):**
  - **Post style:** Slim slender magically-grown wooden column, tapered wider at base
  - **Lamp head geometry:** Open-frame wooden-tine lantern with peaked cap, canonical upright on top of post
  - **Flame container:** Inside open frame, flame visible through tine gaps; housing constant across versions, flame contents evolve (oil → magical → enchanted)
  - Variable per-instance damage profile across v1 (every lamppost in village has different damage story)

- **Setting/lore commitment surfaced mid-session:** Sterling's lampposts were **grown, not built** (Daniel's silhouette #3 framing: "this was grown like this"). Open question for SCOPE.md amendment about extension to other infrastructure. Captured in [`handback-to-main-session.md`](handback-to-main-session.md) Seed 1, elevated from plant-seed parking lot toward SCOPE.md amendment candidate.

- **Asset-progression-as-story-progression seed enriched:** distinct asset versions (not shader-progression), variable damage across instances within v1, diegetic damage philosophy where each defect tells a specific story. v1 lamppost is now the proof-of-concept exemplar. Each future version (v_mid, v_endgame) is a separate `.glb` sharing locked DNA. Flame contents evolve (oil → magical) while housing stays constant.

- **Pipeline executed end-to-end with three new techniques vs mushroom:**
  1. **Multi-mesh hierarchy** — 5 separate child meshes (Post, Lamp, Fire, Sign_arm, Sign_link, Sign) parented under an Empty root (`Lamppost_Root`). Sign's origin set to top edge for swing-animation pivot.
  2. **Emissive lighting** — Emission shader on flame + Point Light at flame's position + Eevee Bloom + Shadow Mode disabled on flame material (so light passes through). Two-element pattern for any glowing light source asset.
  3. **Authored animation** — Action Editor keyframing + F-Modifier cyclic on sign rotation. First Blender animation in the project. Authored in Blender today; runtime procedural sway in Godot is the eventual shipping pattern (logged as Phase 4 follow-up in handback file).

- **Hand-painted textures (5 surfaces, Procreate watercolor brushes):** Sign, Post, Sign_link, Sign_arm, Lamp. All using Wet Acrylic / Wet Ink / Wash brush families for the painterly + cel-shader synthesis. Damage cues (cracks, gouges, broken tine edges, magic-receding tonal shift) painted at texture level rather than modeled in geometry — confirms the "diegetic damage is texture work, not geometry" pipeline decision from session-opener.

- **Renders produced:** Day-lit (lamp unlit per villager-extinguish-at-sunrise design, damage clearly visible in clean daylight) + Night-lit (lamp lit, warm flame against cool ambient, cozy focal mood). Two renders tell two different stories of the same asset — directly illustrating the asset-progression-as-story system in microcosm.

- **Technique learned (highlights captured in [technique-notes.md](technique-notes.md) §Phase 2.3 additions):**
  - Multi-mesh parent-child hierarchy with Empty root
  - Bezier curve + bevel depth + convert-to-mesh for organic curves (rope connector)
  - Extra Objects addon (Add → Curve → Spirals → Archemedian) for parametric spirals
  - Mesh-to-mesh interpenetration is fine for static stylized assets (slight overlap at attachment points sells "connected")
  - Recalculate Normals Outside (Shift+N) fixes Solidify outline appearing in wrong direction
  - Merge by Distance for cleaning coincident verts at primitive caps (triangle-fan cylinders)
  - Cap Fill Type at primitive creation matters: Triangle Fan vs N-Gon affects peak-extrusion ease
  - Smart UV Project at Angle Limit 89° for irregular tubular meshes; manual seam-marking for clean-grain meshes
  - UV orientation isn't automatic — flip via Ctrl+M in UV Editor if texture appears mirrored or upside-down
  - Solidify thickness scales with mesh size; per-asset tuning required
  - Emission alone doesn't cast light in Eevee — need separate Point Light for actual illumination
  - Disable Shadow Mode (or Object → Ray Visibility → Shadow) on emissive material so its own mesh doesn't block its own light
  - Blender 4.x keyframe shortcut change: `I` = insert with current Keying Set; `K` = open property-choice menu
  - Curves cannot be UV-unwrapped without first converting to mesh
  - Mouse-hover context for shortcuts (Ctrl+P, U, I, K) — must be over 3D viewport for most operations
  - Outliner selection conventions are OPPOSITE to Windows: Ctrl+click = individual toggle, Shift+click = range select
  - Watercolor brush family in Procreate produces texture painterly feel naturally — no special technique required, the brush IS the technique
  - F-Modifier (Make Cyclic) in Graph Editor for looping animations

- **Workflow tip discovered:** iCloud Photos folder works as bidirectional PC ↔ iPad transfer shortcut. Save UV PNGs to `C:\Users\[user]\iCloudPhotos\Photos\` on PC; they appear in iPad Photos app automatically. Procreate imports from Photos directly. Export painted textures back via Photos → syncs to PC. Faster than PairDrop for repeated iteration loops.

- **Style-direction checks (final renders):**
  - ✅ Hand-painted watercolor wood textures (Tang/Ghibli/Okami painterly anchor)
  - ✅ Hard cel-shader bands cutting through (Hades graphic structure)
  - ✅ Bold black ink outline (Hades-anchor + Solidify technique)
  - ✅ Emissive flame + Point Light + Bloom (cozy night-lit feel; warm light spilling onto post)
  - ✅ Diegetic damage cues reading at silhouette + texture + lighting levels
  - ✅ Day-lit + night-lit renders tell different stories of same asset
  - ✅ "Grown not built" wooden register reads (organic spiral, tapered base, integrated forms)
  - ✅ Sign swing animation cyclic and stable
  - ⚠️ Damage cracks lean toward pure-black (graphic) rather than dark-walnut-brown (wood-natural). Per-asset taste call; per-pillar acceptable for "magic-receding dramatic" register, could soften for Tang-leaning cozy register on future variants.

- **AI policy notes:** All design decisions Daniel-authored — silhouettes (3 hand-sketched in Procreate), damage philosophy (3 distinct variations articulated), design DNA (3 locked components + variable list), world-state lore ("grown not built" framing), all palette and color choices, all 5 texture surfaces painted from scratch, sign swing animation parameters, render lighting + framing decisions. Claude taught technique, critiqued workflow + style-direction alignment, located human-creator addon (Extra Objects), provided shader/modifier recipes. Did NOT propose specific colors, designs, named entities, paint examples, or pre-author any creative content. Two seeds captured in handback for `/gsd:plant-seed` promotion (asset-progression system, fire sprite character) with explicit Claude-must-not-author notes on the fire sprite.

- **Capability matrix implications (proposed amendments to `16-capability-gap-matrix.md` on return to main GSD):**
  - **"Blender 3D modeling":** advanced (multi-mesh hierarchy + topology cleanup with damage + curve-based modeling + edge loops + bevels)
  - **"Cel-shading shader development":** established (reusable Cel Node Group proven across mushroom + lamppost, 5 different mesh shapes)
  - **"Hand-painted texture work (Procreate)":** established (5 textures painted with watercolor brush family; cel-shader synthesis confirmed)
  - **NEW row: "Blender Action Editor / glTF animation":** basic (first authored animation; cyclic F-Modifier; ready for character-rig progression in Phase 6)
  - **NEW row: "Emissive lighting in Eevee":** basic-established (Emission + Point Light + Bloom + Shadow disable recipe proven; applicable to all glowing-source assets)
  - **NEW row: "Blender curves (Bezier + parametric spirals)":** basic (curve-to-mesh for organic tubular shapes)

- **Phase 2.3 status:** ✅ Complete. **First Colisi-style-aligned ANIMATED prop shipped to art-scratch.**
- **Phase 2 status:** ✅ Complete. All three modules (2.1 donut, 2.2 mushroom, 2.3 lamppost) closed. Daniel has reflexively-internalized the locked-style Blender prop pipeline plus three extensions (multi-mesh, emissive, animation).
- **Next session target:** TBD on Daniel's return. Options: Phase 3 (modular architecture kit-bash) is the syllabus next-in-line; OR return to main GSD work and pick up the survey-distribution Task 3 thread or PROJECT/REQUIREMENTS work. Handback file captures seeds and capability-matrix amendments waiting for surface.

---
